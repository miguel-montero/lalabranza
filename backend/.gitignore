config.php
# vendor/ is intentionally committed — cPanel Git Version Control deploys
# it directly, with no Composer available on the server. Never commit
# config.php above: it holds real database credentials and only ever
# exists on the server, hand-created from config.example.php.
backend/.phpunit.result.cache
